name: deploy infra
description: deploys the base infra

outputs:
  region:
    description: "werfeafc"
    value: ${{ steps.tf_output.outputs.region }}

runs:
  using: "composite"
  steps:
    - name: set op
      id: tf_output
      shell: bash
      run: echo "region=blah">>$GITHUB_OUTPUT
