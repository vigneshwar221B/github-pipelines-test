name: deploy infra
description: deploys the base infra

runs:
  using: "composite"
  steps:
    - name: set op
      id: tf_output
      shell: bash
      run: echo "::set-output name=aws_region::${{ "\"us-east-1\""//\"/ }}"
    - shell: bash
      run: echo ${{ steps.tf_output.aws_region }}
