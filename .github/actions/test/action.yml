name: deploy infra
description: deploys the base infra

runs:
  using: "composite"
  steps:
    - name: set op
      id: tf_output
      shell: bash
      run: |
        echo ${{ github.event.repository.name }}
        echo ${{ vars.TEMP }}

