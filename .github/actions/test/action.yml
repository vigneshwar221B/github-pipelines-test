name: deploy infra
description: deploys the base infra

runs:
  using: "composite"
  steps:
    - name: set op
      id: tf_output
      shell: bash
      run: |
        echo ${{ github.event.repository.name == 'github-pipeline-test' && '-var-file=${{ github.event.repository.name }}' ||'-var-file=${{ github.event.repository.name }}' }}


