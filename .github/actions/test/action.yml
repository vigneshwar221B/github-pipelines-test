name: custom action
description: blah

inputs:
  someVar:
    description: fad
    default: "hi"
    required: false

runs:
  using: "composite"
  steps:
  - name: Print Message
    shell: bash
    run: echo ${{ inputs.someVar }}