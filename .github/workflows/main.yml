name: test

on:
  push:
    branches:
      - main

jobs:
  printa:
    runs-on: ubuntu-latest
    steps:
      - name: Print Message1
        run: echo hi
      - name: Print Message2
        run: jhgfd
      - name: Print Message2
        run: echo hello

  printb:
    runs-on: ubuntu-latest
    steps:
      - name: custom action
        uses: ./.github/actions/test
        with:
          someVar: helo
      - name: Print Message
        run: echo hello