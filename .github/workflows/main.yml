name: test

on:
  push:
    branches:
      - main

jobs:
  printa:
    runs-on: ubuntu-latest
    steps:
      - name: Print Message
        run: echo hi

  printb:
    runs-on: ubuntu-latest
    steps:
      - name: Print Message
        run: echo hello