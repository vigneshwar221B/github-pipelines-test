name: test

on:
  push:
    branches:
      - main

jobs:
  print:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        message: ["Hi", "Hello"]
    steps:
      - name: Print Message
        run: echo ${{ matrix.message }}
